C51 COMPILER V9.60.7.0   SPEECH_PROCESS                                                    02/27/2025 15:23:16 PAGE 1   


C51 COMPILER V9.60.7.0, COMPILATION OF MODULE SPEECH_PROCESS
OBJECT MODULE PLACED IN .\Release\Objects\speech_process.obj
COMPILER INVOKED BY: C:\Keil_v5\C51\BIN\C51.EXE ..\..\App\speech_process.c OPTIMIZE(9,SIZE) BROWSE INTVECTOR(0X8000) INC
                    -DIR(..\..\Libraries\Include;..\..\User;..\..\Hardware;..\..\App) INTERVAL(3) DEBUG OBJECTEXTEND PRINT(.\Release\Listings
                    -\speech_process.lst) OBJECT(.\Release\Objects\speech_process.obj)

line level    source

   1          #include "speech_process.h"
*** WARNING C322 IN LINE 71 OF \BaiduSyncdisk\A_WorkSt\Aworkst\2025-01-22-project250122-����-��Ħ��������-SX384\code\pro
             -ject-250122-TX8C1011\Libraries\Include\debug.h: unknown identifier
   2          
   3          extern volatile bit flag_is_recv_ctl; // 标志位，是否接收了控制命令
   4          
   5          extern volatile bit flag_tim_set_bat_is_low; // 由定时器置位/复位的，表示在工作时，电池
             -是否处于低电量的标志位
   6          extern volatile bit flag_ctl_led_blink;      // 控制标志位，是否控制指示灯闪烁
   7          extern volatile bit flag_ctl_turn_dir;       // 控制标志位，是否更换电机的方向
   8          
   9          extern volatile bit flag_ctl_dev_close; // 控制标志位，是否要关闭设备
  10          
  11          /*
  12              标志位，是否通过语音来切换方向.
  13              用在定时器中断中，判断自动换方向，如果这个标志位置一，
  14              会清除当前自动换方向的计时，重新开始自动换方向的计时
  15          */
  16          extern volatile bit flag_is_turn_dir_by_speech;
  17          
  18          /*
  19              标志位，是否接收到了新的语音信息/有新的按键操作
  20              用在定时器中断，在通过语音关闭设备后，一段时间无操作而彻底关机
  21              如果这个标志位置一，在定时器内会清除自动彻底关机的倒计时，
  22              定时器在清除计时后，会给这个标志位清零
  23          */
  24          extern volatile bit flag_is_new_operation;
  25          
  26          extern volatile bit flag_ctl_low_bat_alarm; // 控制标志位，是否使能低电量报警
  27          
  28          extern void fun_ctl_motor_status(u8 adjust_motor_status);
  29          extern void fun_ctl_heat_status(u8 adjust_heat_status);
  30          
  31          // 初始化控制语音IC电源的引脚
  32          // void speech_ctl_pin_config(void)
  33          // {
  34          //     // P11
  35          //     P1_MD0 |= 0x01 << 2; // 输出模式
  36          // }
  37          
  38          void speech_scan_process(void)
  39          {
  40   1          if (flag_is_recv_ctl)
  41   1          {
  42   2              // 如果接收到了正确的控制命令
  43   2              volatile u8 cmd = 0;
  44   2              flag_is_recv_ctl = 0;
  45   2              cmd = recv_ctl; // 从缓冲区取出控制命名
  46   2      
  47   2              // 表示有新的控制命令，清除在关闭按摩指令后，无操作自动关机的倒计�
             -�
  48   2              flag_is_new_operation = 1;
  49   2              delay_ms(1); // 等待定时器清空对应的（2min没有语音操作而关机）计时变量和
C51 COMPILER V9.60.7.0   SPEECH_PROCESS                                                    02/27/2025 15:23:16 PAGE 2   

             -标志位
  50   2      
  51   2              // (用 if-else if 语句会比switch语句更省空间)
  52   2              // 根据当前设备的情况，对指令进行处理
  53   2              if (CMD_OPEN_DEV == cmd) // 打开按摩/按摩枕/按摩器
  54   2              {
  55   3                  // 如果当前电机是关闭的，才对 开机控制命令进行处理
  56   3                  if (0 == cur_motor_status)
  57   3                  {
  58   4                      alter_motor_speed(2); // 开机后，电机进入二档
  59   4                      motor_pwm_b_enable(); // 电机正向转动
  60   4      
  61   4                      cur_motor_dir = 1;    // 表示当前电机在正转
  62   4                      cur_motor_status = 2; // 更新电机挡位状态
  63   4      
  64   4                      // 如果不处于低电量报警状态，才使能LED闪烁功能：
  65   4                      if (0 == flag_tim_set_bat_is_low)
  66   4                      {
  67   5                          if (0 == cur_ctl_heat_status)
  68   5                          {
  69   6                              // 如果没有打开加热，让绿灯闪烁
  70   6                              LED_RED_OFF();
  71   6                              LED_GREEN_ON();
  72   6                              cur_sel_led = CUR_SEL_LED_GREEN;
  73   6                          }
  74   5                          else
  75   5                          {
  76   6                              // 如果打开了加热，让红灯闪烁
  77   6                              LED_GREEN_OFF();
  78   6                              LED_RED_ON();
  79   6                              cur_sel_led = CUR_SEL_LED_RED;
  80   6                          }
  81   5      
  82   5                          cur_ctl_led_blink_cnt = cur_motor_status; // led闪烁次数与当前电机的挡位�
             -�关
  83   5                          flag_ctl_led_blink = 1;                   // 打开LED闪烁的功能
  84   5                      }
  85   4                  }
  86   3                  else
  87   3                  {
  88   4                      // 电机正在运行，不做处理
  89   4                  }
  90   3              }
  91   2              else if (CMD_CLOSE_DEV == cmd) // 关闭按摩/按摩枕/按摩器
  92   2              {
  93   3                  // flag_ctl_dev_close = 1;
  94   3                  cur_ctl_heat_status = 0; // 关闭加热
  95   3                  motor_pwm_disable();     // 关闭驱动电机的PWM输出：
  96   3                  LED_RED_OFF();
  97   3                  interrupt_led_blink(); // 关闭灯光闪烁的动画
  98   3                  LED_GREEN_ON();        // 点亮绿灯
  99   3                  delay_ms(100);
 100   3                  cur_sel_led = CUR_SEL_LED_GREEN;             // 选中绿灯，准备让它闪烁
 101   3                  cur_ctl_led_blink_cnt = CUR_CTL_LED_BLINK_1; // 闪烁一次
 102   3                  flag_ctl_led_blink = 1;                      // 使能灯光闪烁
 103   3              }
 104   2              else if (CMD_OPEN_HEAT == cmd) // 打开加热
 105   2              {
 106   3                  if (0 == cur_ctl_heat_status)
 107   3                  {
 108   4                      // 如果加热没有开启
 109   4                      // 参数填0，根据全局变量 cur_ctl_heat_status 的状态来自动调节
C51 COMPILER V9.60.7.0   SPEECH_PROCESS                                                    02/27/2025 15:23:16 PAGE 3   

 110   4                      fun_ctl_heat_status(0);
 111   4                  }
 112   3              }
 113   2              else if (CMD_CLOSE_HEAT == cmd) // 关闭加热
 114   2              {
 115   3                  if (0 != cur_ctl_heat_status)
 116   3                  {
 117   4                      // 如果加热已经开启
 118   4                      // flag_ctl_led_blink = 0; // 打断当前正在闪烁的功能
 119   4                      // delay_ms(1);            // 等待定时器中断内部清空闪烁功能对应的标志
             -和变量，否则打断闪灯的效果会变差
 120   4                      interrupt_led_blink();
 121   4                      fun_ctl_close_heat();
 122   4                      delay_ms(100);
 123   4                      cur_sel_led = CUR_SEL_LED_GREEN;
 124   4                      cur_ctl_led_blink_cnt = 1;
 125   4                      flag_ctl_led_blink = 1;
 126   4                  }
 127   3              }
 128   2              else if (CMD_HEAT_FIRST == cmd) // 加热一档
 129   2              {
 130   3                  // if (0 != cur_ctl_heat_status)
 131   3                  {
 132   4                      fun_ctl_heat_status(1);
 133   4                  }
 134   3              }
 135   2              else if (CMD_HEAT_SECOND == cmd) // 加热二档
 136   2              {
 137   3                  // if (0 != cur_ctl_heat_status)
 138   3                  {
 139   4                      fun_ctl_heat_status(2);
 140   4                  }
 141   3              }
 142   2              // else if (cur_motor_status) /* 接下来都是与电机有关的操作，需要电机在运作�
             -�情况下，再做处理，否则不会进入 */
 143   2              // {
 144   2              //     if (CMD_INTENSITY_FIRST == cmd) // 力度一档
 145   2              //     {
 146   2              //         fun_ctl_motor_status(1);
 147   2              //     }
 148   2              //     else if (CMD_INTENSITY_SECOND == cmd) // 力度二档
 149   2              //     {
 150   2              //         fun_ctl_motor_status(2);
 151   2              //     }
 152   2              //     else if (CMD_INTENSITY_THIRD == cmd) // 力度三档
 153   2              //     {
 154   2              //         fun_ctl_motor_status(3);
 155   2              //     }
 156   2              //     else if (CMD_CHANGE_DIR == cmd) // 换个方向
 157   2              //     {
 158   2              //         // 标志位置一，让换方向的操作在主函数执行，因为换方向要间隔
             -500ms
 159   2              //         flag_ctl_turn_dir = 1;
 160   2              //         // 给另外一个标志位置一，让定时器清除自动换方向的计时
 161   2              //         flag_is_turn_dir_by_speech = 1;
 162   2              //     }
 163   2              // }
 164   2      
 165   2      #if 1
 166   2              else if (CMD_INTENSITY_FIRST == cmd) // 力度一档
 167   2              {
 168   3                  if (0 != cur_motor_status)
C51 COMPILER V9.60.7.0   SPEECH_PROCESS                                                    02/27/2025 15:23:16 PAGE 4   

 169   3                  {
 170   4                      // if (0 == cur_motor_status)
 171   4                      // {
 172   4                      //     // motor_forward();
 173   4                      //     motor_pwm_b_enable(); // 电机正向转动
 174   4                      // }
 175   4                      fun_ctl_motor_status(1);
 176   4                  }
 177   3              }
 178   2              else if (CMD_INTENSITY_SECOND == cmd) // 力度二档
 179   2              {
 180   3                  if (0 != cur_motor_status)
 181   3                  {
 182   4                      // if (0 == cur_motor_status)
 183   4                      // {
 184   4                      //     // motor_forward();
 185   4                      //     motor_pwm_b_enable(); // 电机正向转动
 186   4                      // }
 187   4                      fun_ctl_motor_status(2);
 188   4                  }
 189   3              }
 190   2              else if (CMD_INTENSITY_THIRD == cmd) // 力度三档
 191   2              {
 192   3                  if (0 != cur_motor_status)
 193   3                  {
 194   4                      // if (0 == cur_motor_status)
 195   4                      // {
 196   4                      //     // motor_forward();
 197   4                      //     motor_pwm_b_enable(); // 电机正向转动
 198   4                      // }
 199   4                      fun_ctl_motor_status(3);
 200   4                  }
 201   3              }
 202   2              else if (CMD_CHANGE_DIR == cmd) // 换个方向
 203   2              {
 204   3                  // if (0 != cur_motor_status)
 205   3                  if (cur_motor_status) // 电机还在运行，才执行下面语句块
 206   3                  {
 207   4                      // 标志位置一，让换方向的操作在主函数执行，因为换方向要间隔500
             -ms
 208   4                      flag_ctl_turn_dir = 1;
 209   4      
 210   4                      // 给另外一个标志位置一，让定时器清除自动换方向的计时
 211   4                      flag_is_turn_dir_by_speech = 1;
 212   4      
 213   4                      // 让灯光闪烁一次
 214   4                      interrupt_led_blink(); // 打断当前正在执行的LED闪烁功能
 215   4      
 216   4                      // 如果不处于低电量报警状态，才使能LED闪烁功能：
 217   4                      if (0 == flag_ctl_low_bat_alarm)
 218   4                      {
 219   5                          if (0 == cur_ctl_heat_status)
 220   5                          {
 221   6                              // 如果没有打开加热，让绿灯闪烁
 222   6                              LED_RED_OFF();
 223   6                              LED_GREEN_ON();
 224   6                              cur_sel_led = CUR_SEL_LED_GREEN;
 225   6                          }
 226   5                          else
 227   5                          {
 228   6                              // 如果打开了加热，让红灯闪烁
 229   6                              LED_GREEN_OFF();
C51 COMPILER V9.60.7.0   SPEECH_PROCESS                                                    02/27/2025 15:23:16 PAGE 5   

 230   6                              LED_RED_ON();
 231   6                              cur_sel_led = CUR_SEL_LED_RED;
 232   6                          }
 233   5      
 234   5                          cur_ctl_led_blink_cnt = 1; // 指定led闪烁次数
 235   5                          flag_ctl_led_blink = 1;    // 打开LED闪烁的功能
 236   5                      }
 237   4                  }
 238   3              }
 239   2      #endif
 240   2          }
 241   1      }


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =    261    ----
   CONSTANT SIZE    =   ----    ----
   XDATA SIZE       =   ----    ----
   PDATA SIZE       =   ----    ----
   DATA SIZE        =   ----       1
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  1 WARNING(S),  0 ERROR(S)
