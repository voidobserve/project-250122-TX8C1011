C51 COMPILER V9.60.7.0   SPEECH_PROCESS                                                    02/18/2025 17:34:22 PAGE 1   


C51 COMPILER V9.60.7.0, COMPILATION OF MODULE SPEECH_PROCESS
OBJECT MODULE PLACED IN .\Release\Objects\speech_process.obj
COMPILER INVOKED BY: C:\Keil_v5\C51\BIN\C51.EXE ..\..\App\speech_process.c OPTIMIZE(9,SIZE) BROWSE INTVECTOR(0X8000) INC
                    -DIR(..\..\Libraries\Include;..\..\User;..\..\Hardware;..\..\App) INTERVAL(3) DEBUG OBJECTEXTEND PRINT(.\Release\Listings
                    -\speech_process.lst) OBJECT(.\Release\Objects\speech_process.obj)

line level    source

   1          #include "speech_process.h"
*** WARNING C322 IN LINE 71 OF \BaiduSyncdisk\A_WorkSt\Aworkst\2025-01-22-project250122-����-��Ħ��������-SX384\code\pro
             -ject-250122-TX8C1011\Libraries\Include\debug.h: unknown identifier
*** WARNING C322 IN LINE 6 OF ..\..\Hardware\motor.h: unknown identifier
   2          
   3          extern volatile bit flag_is_recv_ctl; // 标志位，是否接收了控制命令
   4          
   5          extern volatile bit flag_tim_set_bat_is_low; // 由定时器置位/复位的，表示在工作时，电池
             -是否处于低电量的标志位
   6          extern volatile bit flag_ctl_led_blink;      // 控制标志位，是否控制指示灯闪烁
   7          extern volatile bit flag_ctl_turn_dir;       // 控制标志位，是否更换电机的方向
   8          
   9          extern volatile bit flag_ctl_dev_close; // 控制标志位，是否要关闭设备
  10          
  11          /*
  12              标志位，是否通过语音来切换方向.
  13              用在定时器中断中，判断自动换方向，如果这个标志位置一，
  14              会清除当前自动换方向的计时，重新开始自动换方向的计时
  15          */
  16          extern volatile bit flag_is_turn_dir_by_speech;
  17          
  18          /*
  19              标志位，是否接收到了新的语音信息/有新的按键操作
  20              用在定时器中断，在通过语音关闭设备后，一段时间无操作而彻底关机
  21              如果这个标志位置一，在定时器内会清除自动彻底关机的倒计时，
  22              定时器在清除计时后，会给这个标志位清零
  23          */
  24          extern volatile bit flag_is_new_operation;
  25          
  26          extern void fun_ctl_motor_status(u8 adjust_motor_status);
  27          extern void fun_ctl_heat_status(u8 adjust_heat_status);
  28          
  29          // 初始化控制语音IC电源的引脚
  30          void speech_ctl_pin_config(void)
  31          {
  32   1          // P11
  33   1          P1_MD0 |= 0x01 << 2; // 输出模式
  34   1      }
  35          
  36          #if USE_MOTOR
              void speech_scan_process(void)
              {
                  if (flag_is_recv_ctl)
                  {
                      // 如果接收到了正确的控制命令
                      volatile u8 cmd = 0;
                      flag_is_recv_ctl = 0;
                      cmd = recv_ctl; // 从缓冲区取出控制命名
              
                      // 表示有新的控制命令，清除在关闭按摩指令后，无操作自动关机的倒计�
             -�
                      flag_is_new_operation = 1;
              
C51 COMPILER V9.60.7.0   SPEECH_PROCESS                                                    02/18/2025 17:34:22 PAGE 2   

              #if 0
                      // 根据当前设备的情况，对指令进行处理
                      switch (cmd)
                      {
                      case CMD_OPEN_DEV: // 打开按摩/按摩枕/按摩器
              
                          // 如果当前电机是关闭的，才对 开机控制命令进行处理
                          if (0 == cur_motor_status)
                          {
                              alter_motor_speed(2); // 开机后，电机进入二档
                              motor_pwm_b_enable(); // 电机正向转动
              
                              cur_motor_dir = 1;    // 表示当前电机在正转
                              cur_motor_status = 2; // 更新电机挡位状态
              
                              // 如果不处于低电量报警状态，才使能LED闪烁功能：
                              if (0 == flag_tim_set_bat_is_low)
                              {
                                  if (0 == cur_ctl_heat_status)
                                  {
                                      // 如果没有打开加热，让绿灯闪烁
                                      LED_RED_OFF();
                                      LED_GREEN_ON();
                                      cur_sel_led = CUR_SEL_LED_GREEN;
                                  }
                                  else
                                  {
                                      // 如果打开了加热，让红灯闪烁
                                      LED_GREEN_OFF();
                                      LED_RED_ON();
                                      cur_sel_led = CUR_SEL_LED_RED;
                                  }
              
                                  cur_ctl_led_blink_cnt = cur_motor_status; // led闪烁次数与当前电机的挡位�
             -�关
                                  flag_ctl_led_blink = 1;                   // 打开LED闪烁的功能
                              }
                          }
                          else
                          {
                              // 电机正在运行，不做处理
                          }
              
                          break;
              
                      case CMD_CLOSE_DEV: // 关闭按摩/按摩枕/按摩器
              
                          flag_ctl_dev_close = 1;
              
                          break;
              
                      case CMD_OPEN_HEAT: // 打开加热
              
                          if (0 == cur_ctl_heat_status)
                          {
                              // 如果加热没有开启
                              // 参数填0，根据全局变量 cur_ctl_heat_status 的状态来自动调节
                              fun_ctl_heat_status(0);
                          }
              
                          break;
              
C51 COMPILER V9.60.7.0   SPEECH_PROCESS                                                    02/18/2025 17:34:22 PAGE 3   

                      case CMD_CLOSE_HEAT: // 关闭加热
              
                          if (0 != cur_ctl_heat_status)
                          {
                              // 如果加热已经开启
                              flag_ctl_led_blink = 0; // 打断当前正在闪烁的功能
                              delay_ms(1); // 等待定时器中断内部清空闪烁功能对应的标志和变量，�
             -�则打断闪灯的效果会变差
                              fun_ctl_close_heat();
                          }
              
                          break;
              
                      case CMD_CHANGE_DIR: // 换个方向
              
                          if (0 != cur_motor_status)
                          {
                              // 标志位置一，让换方向的操作在主函数执行，因为换方向要间隔500
             -ms
                              flag_ctl_turn_dir = 1;
              
                              // 给另外一个标志位置一，让定时器清除自动换方向的计时
                              flag_is_turn_dir_by_speech = 1;
                          }
              
                          break;
              
                      case CMD_INTENSITY_FIRST: // 力度一档
              
                          if (0 != cur_motor_status)
                          {
                              fun_ctl_motor_status(1);
                          }
              
                          break;
              
                      case CMD_INTENSITY_SECOND: // 力度二档
              
                          if (0 != cur_motor_status)
                          {
                              fun_ctl_motor_status(2);
                          }
              
                          break;
              
                      case CMD_INTENSITY_THIRD: // 力度三档
              
                          if (0 != cur_motor_status)
                          {
                              fun_ctl_motor_status(3);
                          }
              
                          break;
              
                      case CMD_HEAT_FIRST: // 加热一档
              
                          if (0 != cur_ctl_heat_status)
                          {
                              fun_ctl_heat_status(1);
                          }
              
                          break;
C51 COMPILER V9.60.7.0   SPEECH_PROCESS                                                    02/18/2025 17:34:22 PAGE 4   

              
                      case CMD_HEAT_SECOND: // 加热二档
              
                          if (0 != cur_ctl_heat_status)
                          {
                              fun_ctl_heat_status(2);
                          }
              
                          break;
                      }
              #endif
              
                      // (用 if-else if 语句会比switch语句更省空间)
                      // 根据当前设备的情况，对指令进行处理
                      if (CMD_OPEN_DEV == cmd) // 打开按摩/按摩枕/按摩器
                      {
                          // 如果当前电机是关闭的，才对 开机控制命令进行处理
                          if (0 == cur_motor_status)
                          {
                              alter_motor_speed(2); // 开机后，电机进入二档
                              motor_pwm_b_enable(); // 电机正向转动
              
                              cur_motor_dir = 1;    // 表示当前电机在正转
                              cur_motor_status = 2; // 更新电机挡位状态
              
                              // 如果不处于低电量报警状态，才使能LED闪烁功能：
                              if (0 == flag_tim_set_bat_is_low)
                              {
                                  if (0 == cur_ctl_heat_status)
                                  {
                                      // 如果没有打开加热，让绿灯闪烁
                                      LED_RED_OFF();
                                      LED_GREEN_ON();
                                      cur_sel_led = CUR_SEL_LED_GREEN;
                                  }
                                  else
                                  {
                                      // 如果打开了加热，让红灯闪烁
                                      LED_GREEN_OFF();
                                      LED_RED_ON();
                                      cur_sel_led = CUR_SEL_LED_RED;
                                  }
              
                                  cur_ctl_led_blink_cnt = cur_motor_status; // led闪烁次数与当前电机的挡位�
             -�关
                                  flag_ctl_led_blink = 1;                   // 打开LED闪烁的功能
                              }
                          }
                          else
                          {
                              // 电机正在运行，不做处理
                          }
                      } 
                      else if (CMD_CLOSE_DEV == cmd) // 关闭按摩/按摩枕/按摩器
                      {
                          flag_ctl_dev_close = 1;
                      }
                      else if (CMD_OPEN_HEAT == cmd) // 打开加热
                      {
                          if (0 == cur_ctl_heat_status)
                          {
                              // 如果加热没有开启
C51 COMPILER V9.60.7.0   SPEECH_PROCESS                                                    02/18/2025 17:34:22 PAGE 5   

                              // 参数填0，根据全局变量 cur_ctl_heat_status 的状态来自动调节
                              fun_ctl_heat_status(0);
                          }
                      }
                      else if (CMD_CLOSE_HEAT == cmd) // 关闭加热
                      {
                          if (0 != cur_ctl_heat_status)
                          {
                              // 如果加热已经开启
                              flag_ctl_led_blink = 0; // 打断当前正在闪烁的功能
                              delay_ms(1);            // 等待定时器中断内部清空闪烁功能对应的标志和
             -变量，否则打断闪灯的效果会变差
                              fun_ctl_close_heat();
                          }
                      }
                      else if (CMD_CHANGE_DIR == cmd) // 换个方向
                      {
                          if (0 != cur_motor_status)
                          {
                              // 标志位置一，让换方向的操作在主函数执行，因为换方向要间隔500
             -ms
                              flag_ctl_turn_dir = 1;
              
                              // 给另外一个标志位置一，让定时器清除自动换方向的计时
                              flag_is_turn_dir_by_speech = 1;
                          }
                      }
                      else if (CMD_INTENSITY_FIRST == cmd) // 力度一档
                      {
                          if (0 != cur_motor_status)
                          {
                              fun_ctl_motor_status(1);
                          }
                      }
                      else if (CMD_INTENSITY_SECOND == cmd) // 力度二档
                      { 
                          if (0 != cur_motor_status)
                          {
                              fun_ctl_motor_status(2);
                          }
                      }
                      else if (CMD_INTENSITY_THIRD == cmd) // 力度三档
                      {
                          if (0 != cur_motor_status)
                          {
                              fun_ctl_motor_status(3);
                          }
                      }
                      else if (CMD_HEAT_FIRST == cmd) // 加热一档
                      {
                          if (0 != cur_ctl_heat_status)
                          {
                              fun_ctl_heat_status(1);
                          }
                      }
                      else if (CMD_HEAT_SECOND == cmd) // 加热二档
                      { 
                          if (0 != cur_ctl_heat_status)
                          {
                              fun_ctl_heat_status(2);
                          }
                      }
C51 COMPILER V9.60.7.0   SPEECH_PROCESS                                                    02/18/2025 17:34:22 PAGE 6   

                  }
              }
              #endif


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =      7    ----
   CONSTANT SIZE    =   ----    ----
   XDATA SIZE       =   ----    ----
   PDATA SIZE       =   ----    ----
   DATA SIZE        =   ----    ----
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  2 WARNING(S),  0 ERROR(S)
